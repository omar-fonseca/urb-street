# Solo el host del proyecto (SIN /rest/v1 al final)
# Ejemplo: https://xxxx.supabase.co
VITE_SUPABASE_URL=
# anon / publishable key (NUNCA service_role)
VITE_SUPABASE_PUBLISHABLE_KEY=

# Solo local — validación 1 imagen (scripts/validate-one-image.mjs). NO uses prefijo VITE_.
# ADMIN_EMAIL=
# ADMIN_PASSWORD=
