# Solo el host del proyecto (sin /rest/v1)
# Ejemplo: https://xxxx.supabase.co
VITE_SUPABASE_URL=

# anon / publishable key — nunca service_role
VITE_SUPABASE_PUBLISHABLE_KEY=

# Solo local (scripts de validación). Sin prefijo VITE_. No usar en Cloudflare.
# ADMIN_EMAIL=
# ADMIN_PASSWORD=
